/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // output: "export",
  // distDir: "out",
   compiler: {
    styledComponents: true,
  },
};

module.exports = nextConfig
